# HOMEWORK-12

+ logistic regression evaluation

## REQUIREMENTS

1. meson > 0.58
3. ninja

# meson setup and run

1.  Clone repo `git clone https://github.com/Kerosin3/meson-cpp.git --branch HW12 --single-branch`
2.  Setup build `meson setup build`
3.  Compile build `meson compile -C build`
4.  Run executable `./build/fashion_mnist test/test.csv test/logreg_coef.txt`
